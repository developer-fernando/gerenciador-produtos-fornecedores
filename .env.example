# Variáveis do docker-compose (copie para .env: cp .env.example .env)

# Banco de dados (MySQL)
DB_DATABASE=horizon
DB_USERNAME=horizon
DB_PASSWORD=secret
DB_ROOT_PASSWORD=root

# Portas expostas no host
DB_PORT=3306
BACKEND_PORT=8000
FRONTEND_PORT=5173
